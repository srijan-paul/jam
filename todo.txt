TODO:

- Verify that the unary operator operand is SIMPLE (see spec).

Parser:
- Diagnostics can now store messages directly, I guess.
	- Remove MessageIndex enum.
- Parse arrow function expressions.
- Remove all calls to `peek` (we're not actually peeking...)
- Rename lookahead -> cur everywhere

Ast:
- Rename SubRange
- Use `object_property` for object literals and `property_pattern` for object patterns.

Tokenizer:
- Tokenize Octal literal.
- Tokenize Template literal.
- Tokenize Regex literal.

- Detect identifier and argument name clash.
- Cover grammar for arrow functions and parenthesized expressions.
- store numeric value of numeric literals.

- No escape sequence in keywords


Tests:
- Proper regression testing on CI for test-262
